# These are used with sudo priviliges!
# Use this for connecting SIM800 to 2G network. Add the following line to "sudo crontab -e".
# Otherwise, this is not needed!

#@reboot /home/pi/startsakis.sh >/dev/null 2>&1
